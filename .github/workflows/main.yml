name: Run Tests and Coverage

on:
  push:
    branches:
      - main

jobs:
  test:
    runs-on: ubuntu-latest

    steps:
    - name: Checkout code
      uses: actions/checkout@v2

    - name: Set up Node.js
      uses: actions/setup-node@v4
      with:
        node-version: 14

    - name: Install dependencies
      run: npm ci

    - name: Set working directory to sw-test-app
      run: cd sw-test-app

    - name: Run tests with coverage
      run: npm run test_c8
      working-directory: .

    - name: Upload coverage to Coveralls
      run: npm install coveralls && npx coveralls
